#include "main.h"

/**
 * _strpbrk - function locates the first occurrence in the string s
 * @s: first input
 * @accept: second input
 * Return: accept (pointer to byte in s), NULL (if no such byte is found)
 */
char *_strpbrk(char *s, char *accept)
{
	int k 
